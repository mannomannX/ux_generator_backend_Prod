{
  "name": "@ux-flow/api-gateway-service",
  "version": "1.0.0",
  "description": "API Gateway service for UX-Flow-Engine with WebSocket support",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:integration": "jest --config=tests/integration/jest.config.js",
    "lint": "eslint src/**/*.js",
    "lint:fix": "eslint src/**/*.js --fix",
    "build": "echo 'No build step required'",
    "clean": "echo 'No clean step required'"
  },
  "dependencies": {
    "@ux-flow/common": "file:../../packages/common",
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.2.0",
    "ws": "^8.17.0",
    "bcrypt": "^5.1.1",
    "joi": "^17.12.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}